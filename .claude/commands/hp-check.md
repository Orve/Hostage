Supabaseの現在のペット（キャラクター）HP状態を確認してほしいのだ。

以下を確認してほしいのだ：

1. `app/services/supabase.py` のクライアント設定を確認
2. バックエンドが起動中であれば、ペット一覧を取得:
   ```bash
   curl http://localhost:8000/pets
   ```
3. HPが低いペット（HP < 30）があれば警告を表示
4. 死亡状態（HP = 0）のペットがあれば赤字で表示
5. `app/services/game_logic.py` の減衰計算ロジックを確認し、次回cron実行時の予測ダメージを計算

結果をわかりやすく表形式で表示してほしいのだ。
